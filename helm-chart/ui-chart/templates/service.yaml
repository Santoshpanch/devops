apiVersion: v1
kind: Service
metadata:
 name: {{ include "ui.fullname" . }}
spec:
 selector:
  {{- include "ui.labels" . | nindent 2 }}
 ports:
 - port:
    {{ .Values.service.port }}
   targetPort:
    {{ .Values.service.targetPort }}
 type:
  {{ .Values.service.type }}
