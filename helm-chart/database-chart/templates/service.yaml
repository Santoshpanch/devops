apiVersion: v1
kind: Service
metadata:
 name:
  {{ include "database.fullname" . }}
spec:
 clusterIP: None
 selector:
  {{- include "database.labels" . | nindent 2 }}
 ports:
 - port:
    3306
   targetPort:
    3306
