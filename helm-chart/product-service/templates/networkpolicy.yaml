apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
 name:
  {{ include "product-service.fullname" . }}-networkpolicy
spec:
 podSelector:
  matchLabels:
   {{- include "product-service.labels" . | nindent 4 }}
 policyTypes:
 - Ingress
 - Egress
 ingress:
 - from:
   - podSelector:
       matchLabels:
        app: frontend-ui
   ports:
   - protocol: TCP
     port: 8082
 egress:
 - ports:
   - protocol: TCP
     port: 5432
