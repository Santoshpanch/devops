apiVersion: v1
kind: Secret
metadata:
 name: {{ include "user-service.fullname" . }}-secret
type: Opaque
stringData:
  DB_USERNAME: {{ .Values.databaseSecret.username }}
  DB_PASSWORD: {{ .Values.databaseSecret.password }}
