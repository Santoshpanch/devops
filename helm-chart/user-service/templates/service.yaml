apiVersion: v1
kind: Service
metadata:
 name:
  {{ include "user-service.fullname" . }}
spec:
 type: ClusterIP
 selector:
  {{- include "user-service.labels" . | nindent 2 }}
 ports:
 - port:
     {{ .Values.service.port }}
   targetPort:
     {{ .Values.service.targetPort }}
